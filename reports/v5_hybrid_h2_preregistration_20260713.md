# H2 Immutable Preregistration

Status: `REGISTERED_NO_LAUNCH`. H2 tests one variable: treatment replaces critic
returns on eligible non-fold showdown rows with exact-or-deterministic-K200 conditional
line values. Actor rewards, actor GAE advantages, policy loss, architecture, optimizer,
deal stream, opponent assignment, EXP-002, and priors `0.01/0.02` remain unchanged.

Both arms start from exact gate31400 critic-v1 SHA
`bcbb46bd01d62fcdea602269b7047323315d4e9bbcf447ea0323e289fde11a8e` and run
exactly 20M actual hands, control first. The treatment endpoint is adopted only if all
registered variance/bias, endpoint-MSE, 40k-pair internal mirror, throughput, entropy,
health, provenance, and identity gates pass.

The prospective power review amended the draft's 20k mirror sample to a fixed 40k
pairs before registration. No adaptive extension, second seed, later checkpoint, or
threshold change is allowed. H2 authorizes zero official Slumbot hands and no strength
claim. A terminal H2 FAIL/no-progress after H1 triggers the autonomous route review.
