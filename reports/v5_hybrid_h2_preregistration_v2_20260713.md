# H2 Immutable Preregistration v2

This v2 incorporates the full v1 contract by exact SHA and changes only the bound
trainer hash/implementation audit after adding fail-closed arm identity checks. Every H2
arm must now hash-bind this preregistration and its design lock, and its declared arm
must agree with the showdown-target flag.

No H2 arm or result existed when this correction was made. The single variable, exact
gate31400 source, fixed 20M-per-arm windows, fixed 40k mirror pairs, thresholds,
rollback, zero official hands, and terminal rules are unchanged.
