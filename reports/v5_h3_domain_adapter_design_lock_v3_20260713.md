# H3-DOMAIN-ADAPTER-001-V3-SNAPSHOT

V2 remains terminal FAIL and its bundle stays forbidden. V3 changes only the offline
state/mapping boundary exposed by the frozen V2 failure: it exports the exact Path-1
source-tree snapshot and constructs the synthetic v5.5 state directly rather than
replaying deep histories through a different state engine.

Sized source actions are mapped by exact amount to the closest *actual legal* non-all-in
v5.5 action. Every nominal-to-legal projection and its amount error is recorded. No mass
may be dropped, renormalized, filtered by policy probability, or redirected to all-in.

The first-board double-run smoke and the full selected-corpus projection-risk thresholds
are frozen in the matching JSON. This is asset engineering only and grants no dataset,
H3 behavior, official-hand, or strength authority.
