# HYBRID H2 Preregistration Draft

Status: **DRAFT_NO_LAUNCH**. Official Slumbot hands authorized: **0**.

H1 ended FAIL, so critic_v2 is rejected and H2 selects the exact gate31400 critic_v1 rollback source. H2 changes one variable: add K=200 deterministic runout-averaged critic targets for eligible decision rows on all non-fold showdown lines. Actor rewards, actor GAE advantages, policy loss, architecture, pool, priors and deal stream remain unchanged.

Fixed design: 20M actual hands per same-start arm, control then treatment, no extension/second seed/later endpoint. Primary PASS needs target-variance reduction point >=30% and bootstrap95 lower >=20%, with tight mean-bias guards. Endpoint critic MSE, 20k-pair greedy internal mirror non-inferiority, h/s>=0.85, entropy and health are mandatory guards.

This is a draft only. Implementation, H2-VAR-001, immutable registration/audit, design lock, preflight, watchers and all launches remain unauthorized.