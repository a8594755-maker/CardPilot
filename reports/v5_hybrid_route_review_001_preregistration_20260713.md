# HYBRID-ROUTE-REVIEW-001 preregistration

This is a conditional reporting/decision registration. It does not launch behavior or authorize official hands.

It triggers only if H1 remains terminal FAIL and the exact H2 judgment is a valid registered FAIL or fixed-sample INCONCLUSIVE with `route_review_required=true`. Missing evidence is not a terminal no-progress verdict. H2 PASS continues the default H3 route without invoking the two-window review.

The first eligible route remains H3 actor-only CFR policy distillation, but only after the corrected legal-all-in successor reaches 600 QA-PASS boards and an exact v55/9-action bridge passes. Until then, only engineering prerequisites may continue. H4 requires a complete common-deal cross-play matrix and registered pool measurement. H5 requires resolver legality, latency and compatible-blueprint proof. Pending prerequisites are not route exhaustion.

No raw realized-loss or action-frequency narrative may select an intervention. Every selected behavior window still needs its own immutable preregistration, design lock, preflight and fail-closed judgment.
