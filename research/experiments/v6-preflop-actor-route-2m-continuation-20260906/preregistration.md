# Preregistered unchanged-regimen actor-route 1M to 2M continuation

This experiment follows the completed fixed80k external calibration, not a new
algorithm arm or promotion. External connected-minus-detached was +20.3918 and
-14.9258 bb/100 across seeds, both contrasts imprecise; all four absolute scores
were negative. Internal positive direction did not consistently transfer.
Hypothesis: a bounded additional geometric dose may distinguish delayed learning
from nonreproducible short-run changes. More hands are not assumed to help.

## Fixed allocation
Connected primary family and detached control, seeds1 and3. Four existing final
stage2 checkpoints are continued, never reset, to2,097,152 physical hands AFTER
the original actor split. Absolute targets are12,595,386 for seed1 and12,589,293
for seed3. Starting physical counts: S1 detached11,547,070, connected11,551,174;
S3 detached11,542,774, connected11,543,121. Minimum new physical total4,185,219;
count actual completed overshoot, not nominal or replay rows.
Fixed sequential order S1 detached, S1 connected, S3 connected, S3 detached.
No additional interim endpoint or algorithm variant. Preserve all86 Adam states,
actual LR9.999999999999996e-05, replay depth2/ratio0.5, pool, counter offsets,
main RNG, prefixes, source-reference and actor-route origin. The CLI LR0.0003
is NOT a reset: preserve-resumed-optimizer-lr retains the actual checkpoint LR.
No new route migration. Frozen wrapper/runtime remain the exact prior sources.
New durable per-attempt namespaces qualify statistical worker continuation,
not bitwise worker/in-flight RNG equivalence. Earlier unknown crash tail stays
unknown; no skipped index or inherited/replay hand is counted as new training.
Two-hour per-child runtime safety bound is unchanged; a subtarget safe boundary
requires preserved-state review, not automatic reset/retry.

## Fixed internal terminal check
Only one endpoint check:512 new common-deck pairs per each of four frozen
anchors, greedy physical200bb / legacy-v4 observation, both seats. Four policies
produce32,768 executions on4,096 unique common decks shared within seed across
arms. Original actor split parent stays the common control for both arms.
Evaluation seeds are20264411 and20264431. Check against the frozen inventory of
prior common decks. Standard10 is also a training reference; these anchors are
not all unseen opponents. Conditional nominal paired-deck intervals quantify
deck noise, not training-seed population uncertainty or external strength.
The inherited severe broad-collapse rule is reported, not replaced with a small
positive pooled-CI requirement. No model is promoted from these internal scores.

## Decision and evidence
Keep all four endpoints. On valid completion, separately preregister fixed fresh
external calibration of all four before allocating more training. No automatic
4M/8M/paper-scale or final100k launch. Repeated broad regression or continued
proxy/external disagreement at the additional dose argues for changing general
training objective/opponent distribution, not endless extension. Inconclusive
small effects remain evidence shortage, not global method disproof.
Reuse qualified trainer/executor/state inspectors. Qualify only changed command
bindings, target accounting, fresh namespaces and smaller evaluator schedule.
Log actual commands, state/hash provenance, actual hands/replay, raw internal
decks, process identities/exits, errors and resource wall time in this one record.
Controller owns logger while live. No other writer; no Slumbot labels or special
action rules. If a process disappears, inspect retained state and evidence before
any continuation. No closed experiment or old raw hand is rerun/overwritten.

