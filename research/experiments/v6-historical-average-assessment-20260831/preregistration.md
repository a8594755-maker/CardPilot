# Fixed epoch12 historical-average assessment

Prepared before any distillation fit outcome, during its frozen teacher data
collection. Do not run before parent v6-historical-average-distillation-20260831
is COMPLETED with independent reviewed_analysis PASS and decision
ADMIT_SEPARATE_STRENGTH_DIAGNOSTIC. Freeze only its exact epoch12/latest byte hash,
never an earlier epoch. A failed fit gate leaves this assessment unexecuted.

This experiment is entirely offline: no Slumbot calls. It performs candidate-
specific canonical native/public execution parity and an independent internal
diagnostic. Neither internal return nor checkpoint ranking selects the candidate
or admits100k. Valid evidence here admits a separately preregistered20k pilot.

Parity: the existing immutable512model-independent trajectories/4202states from
v6-common-state-retention-diagnostic-20260831, CPU sampled temperature1, shared
uniform seed2026101102. Compare exact observations, full9-slot legal action table,
all probabilities, sampled slot and increment using the live-audited READY runtime.
Cover all8street/seat strata;8404policy-state calls,0new unique hands. Preserve
parity rows, runtime hashes and source/copy matching to the actual distillation.

Internal diagnostic: student and original corrected-v6 source, each against the
same five fixed anchors from v6-selfplay75-transfer-pilot-r4-20260831. Use4096
mirrored pairs per cell,10cells=81920evaluation hands, seed2026101101, same full52
decks and physical-seat keyed action uniforms across all cells. Maximum6concurrent
CPU one-thread clients. Known anchor3/4 families are diagnostic, not novel unseen
opponents. No model updates, search, action overrides or return-dependent stopping.

Independently regenerate all4096decks; require every cell's contiguous pair indices,
identical decks, declared frozen model hashes, complete8192hands, finite bounded
returns and exact source-vs-source zero. Use paired difference of MIRRORED PAIR
averages for student-minus-source against each anchor. Report ordinary normal95%
and Bonferroni family5 simultaneous95%CIs, including every anchor result. No
pairing across unrelated real Slumbot deals or comparison to old v5 baselines.

Finish the same record after terminal-process, data arithmetic and identity review.
If parent fit, parity and all internal evidence are valid, decision
ADMIT_SEPARATE_FRESH20K regardless of internal return. These diagnostics cannot
establish the Goal and do not themselves request a live session. A future20k
pilot must use8fresh2500-hand sessions, disjoint tokens/seeds from all prior real
cohorts, strict journaled CPU sample mode and full model-action replay. Only a
valid positive pilot point may allocate a separate fresh100k qualification.

Exact entry point once the parent is ready:
python research/experiments/v6-historical-average-assessment-20260831/run_assessment.py
