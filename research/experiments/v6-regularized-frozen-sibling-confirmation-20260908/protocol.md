# Independent frozen sibling-panel confirmation

Freeze both seed1/3 final65536 regularized and control endpoints and their original
parents from the completed regularized-return pilot. No new training or selection
of the best seed. SampledT1, physical200bb, legacy-v4 observations, existing CUDA
single-hand evaluator. Exactly512 new decks per seed/anchor, three policies,
both seats:49152 physical evaluation executions,8192 unique decks. Common decks
and physical-player decision random numbers within each comparison. New namespace
distinct from all pilot training and evaluation; scan available prior raw decks.

Panel, fixed before collecting reward:standard10,cfr4,legacy_iter16,legacy_mixed65k
as preservation; moving_s1,moving_s3,half_lr_s1,half_lr_s3 as sibling transfer.
The latter identities come from the prior outcome-blind heldout qualification.
They were not used by the fixed Standard10-only pilot, but share ancestors and
do NOT establish universal or lifetime-unseen generalization. No panel selection
based on new endpoint scores. No extra cohort, early success stop or eta retuning.

Report paired deck-level average over both seats, per seed, treatment-control and
treatment-parent as the four primary contrasts. Ordinary95% and Bonferroni95%
family intervals for these four; control-parent, individual seats/anchors/panels
are descriptive. Exact original8-anchor pooled result is not being replicated:
the preservation subpanel is repeated independently, transfer opponents differ.

Confirmation requires both treatment-control ordinary95% lower bounds positive
with positive sibling-transfer point estimates, and no replicated parent-relative
regression. Report family-adjusted results without hiding failure to clear them.
Do not call relative control advantage a replicated parent improvement unless
both parent-relative intervals support that separate claim. Insufficient evidence
is not long-run method rejection. Complete run and inspect tails/seat concentration
before allocating external calibration or further geometric learning.

Zero Slumbot, zero final benchmark hands. Existing external runtime is primarily
greedy; sampled external contract must be explicitly qualified before that later
calibration. No promotion or paper-scale launch from this protocol alone.
