# Frozen Deep CFR snapshot policy versus Standard10

Decision: `REJECT_CURRENT_DEEP_CFR_SNAPSHOT_TRANSFER`.

The exact frozen K4 surrogate snapshot bundle and frozen Standard10 checkpoint
completed 4,096 common-deck seat pairs (8,192 physical-v6 hands). The candidate
kept its LCFR-weighted per-hand snapshot plus regret-action sampling contract;
Standard10 kept the audited legacy-v4 observation bridge and historical greedy
execution. No action repair, override, checkpoint selection, or Slumbot access
occurred.

Candidate result: -473.51 bb/100 with paired 95% CI
[-702.72, -244.30]. The interval is wholly negative. Although 2,121 pair means
were positive versus 1,408 negative and 567 zero, the negative tail dominated
value. This is decisive evidence that the current from-zero Deep CFR policy is
far below the closest mature learned reference despite occasionally positive
simple-anchor development means.

No Slumbot adapter or external gate is admitted. The result also confirms the
historical held-out proxy warning: weak fixed-policy panels can rank or reward
pathological exploitative policies that fail against a learned reference.

The from-zero exact-v6 Deep CFR route is closed at its current architecture and
budget. A future regret-based route would need to preserve or initialize from a
mature learned policy and prove learned-anchor parity before scaling. Prior
single-teacher imitation fresh20k evidence around -18.50 bb/100 and Standard10's
own negative fresh20k mean must remain explicit; neither is a solved direction.
