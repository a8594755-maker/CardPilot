# Fixed two-seed historical-opponent execution mixture trial

Primary family: existing full-network preflop-connected, critic-v2, replay,
static-reference-KL, anchor-latest PPO lineages. One control only: unchanged
sampled historical opponents. Treatment: per-decision 0.5 original categorical
plus 0.5 legal greedy distribution for historical-pool requests. Both continue
to sample hero/self-play at T1; no Slumbot-derived opponent or action label.

Both arms start from the identical current recent-stage2 parent within Seed1
and Seed3. The completed 36,079-hand mechanism smokes are retained separately;
this is new managed-namespace exposure, not replay of those deals or deletion of
their training. The smokes are not pooled into the matched dose or strength claim.

Additional physical dose per branch: 262,144 then 1,048,576, measured relative
to Seed1 13,645,045 and Seed3 13,641,556 physical counters. Whole-update overshoot
is included in accounting. Four branches total about 4.2M new physical hands.
Every stage preserves optimizer LR/state, replay/RNG, counters and raw prefixes;
new receipt namespaces establish statistical, not bitwise worker continuation.
Mixture runtime sidecar SHA and weight are verified before stage2 and attached to
the parent contract and endpoint verification. Checkpoint alone cannot restore
this additional setting. No reset, retry or intervention on live source.

Stage1 order: S1 control, S1 mixture, S3 mixture, S3 control; stage2 reversed.
Inherited settings include 12x8 rollout environments,4096 transition hands/update,
16384 minibatch,2 PPO epochs,targetKL.01,actual LR~1e-4,static Std10 KL1,replay.5
with depth2,K5 anchor-latest,selfplay.25,adaptive8groups,GN and physical200bb.
Each job has a 7200s natural boundary. Boundary below target preserves evidence
and requests research review rather than retry. Ordinary negative returns do not
stop an arm midstage. Only inherited preregistered broad-collapse or integrity
failures stop further allocation; finite noisy smoke scores do not kill a family.

After each stage evaluate all four fixed endpoints against each own original
parent on untouched common decks, four frozen heterogeneous anchors and both
seats. 2048 pairs/anchor,32768 executed hands/endpoint,131072 executions/stage;
32768 unique planned decks over both stages are prechecked against retained
corpus. Equal seeds within a seed/stage permit paired contrasts, not paired
training randomness. Evaluator remains CPU/GPU-network generic greedy legacy-v4
physical200bb as in the qualified predecessor; no endpoint mode tuning.

Report own-parent slopes, treatment/control deltas, anchors and seats for both
seeds, sample uncertainty and physical/transition/replay costs separately.
Conditional intervals are not training-seed-population inference. Do not select
an isolated favorable bucket. Fixed1M ends in research review, not automatic
external80k, final100k or paper-scale launch. Compare breadth and dose slopes to
decide bounded4M continuation or rejection; prior small-dose negatives alone do
not establish impossibility at scale. Slumbot budget for this record is zero.
