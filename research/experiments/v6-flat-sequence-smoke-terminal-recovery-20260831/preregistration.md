# Flat-sequence smoke terminal-evidence recovery

Parent experiment v6-flat-sequence-reward-smoke-20260831 remains FAILED. Its child
trainer nevertheless exited0 after35546 physical environment hands and preserved
endpoint SHA256
e0dea568045508705ce24e4b54e0514eee14051225b5de031b3d1d13d0083a9e. The parent
wrapper then failed before session audit or evaluation because it required stale
top-level policy_loss/value_loss fields. No training process remains and this
record must never resume, modify or replace that endpoint.

Read-only recovery: verify the parent's captured execution sources, exact input and
endpoint hashes, v6 physical accounting, seven contiguous metrics rows using actual
schema fields (approx_kl, entropy, reference_policy_kl and all serialized numeric
values finite),100 model tensors,14 flat-sequence tensors,20 optimizer states,
frozen non-value source tensors, and run the existing terminal fixed-pool session
audit against target32768/final iteration7/pool3/archive4/global normalization.

Only after every audit passes, copy the exact endpoint and untouched anchor0 into
this record and execute the parent's captured v6_mirror_eval on4096 never-before-run
mirrored pairs at fixed seed20261032. Compute bb/100 and ordinary pair-level CI from
raw JSONL. Preserve the parent's original smoke gate: point estimate>-20bb/100 plus
all health/audit requirements -> ADMIT_MATCHED_FLAT_SEQUENCE_REWARD_PILOT;
otherwise FLAT_SEQUENCE_REWARD_SMOKE_NOT_PROMISING. Zero new training or Slumbot
hands. This cannot retroactively change the parent FAILED status and is not external
strength evidence.
