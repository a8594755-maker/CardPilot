# Bounded unchanged-regimen 1M to 2M continuation

Continue seeds1/3 from original fresh-only trial replay-control stage2 checkpoints,
not gradient-probe descendants. Add1048576 actual physical hands per seed,
whole-iteration overshoot counted. Preserve full-network/connected-preflop actor,
detached critic trunk, static Standard10 current-to-reference KL1, inherited Adam
and LR, replay ratio0.5/buffer2/RNG, cap9 anchor-latest pool and allocation,
sampled hero/opponents T1. Disable optional gradient observation (parent default).
Single trained family, frozen comparisons only; no new algorithmic arm.

Each training job capped7200seconds; no automatic retry or counter reset. Initial
serialized model/Adam/replay/pool/reference state checked with existing lifecycle;
new managed deal namespace, immutable copied metric/assignment prefixes. Record
physical, transition and replay separately. This is statistical continuation,
not full worker RNG/inflight equivalence or proven unique training-deck coverage.

After both jobs complete, evaluate each endpoint against both extension-start
checkpoint and earlier family-allocation flat root. Same eight anchors as prior
fresh trial, split preservation(first4) and transfer(last4); shared ancestry,
not lifetime-unseen. Four matched evaluator jobs,1024 mirrored decks/anchor,
32768 executions/job,131072 total; duplicated endpoint executions deliberately
counted and checked identical across the two baseline jobs. Both seats,
physical200bb legacy_v4 greedy. Fixed evaluator seed202609082000+100*seed,
anchor offset1000003. Exclude planned decks from prior inventoried raw corpus
including temporal aggregation evaluation; preserve all prior evidence.

No outcome-dependent stopping, checkpoint selection or sample extension. Report
endpoint-minus-extension-start and endpoint-minus-earlier-root, each seed/panel/
anchor/seat, paired normal1.96 CIs (conditional/unadjusted), throughput and costs.
No automatic4M or Slumbot. Consider further scale only if both contrasts have
positive pooled and panel point estimates in both seeds, positive both-seat point
estimates, no panel with >=3 anchor upperCI<-25 and both-seat upperCI<0, with
affordable measured cost. These exploratory allocation conditions are not proof
of strength; independent external calibration is required before major expansion.

Rationale: prior updates active, no replicated global actor-gradient cancellation,
and previous endpoint estimates inconclusive. This is one bounded test of longer
fixed-regimen learning, not an assertion that a positive slope already exists.
If evidence remains flat/inconsistent, revise the learning approach rather than
an automatic longer run or another pool/replay/ensemble sweep.
