# Matched causal-TCN terminal recovery

The parent matched pilot completed both fixed training arms and all fifteen
4,096-pair evaluation cells, then its wrapper received Windows WinError5 while
atomically replacing execution.json before aggregation. The parent is closed
FAILED and its evidence remains immutable.

This recovery is strictly read-only with respect to parent evidence. Generate no
training or evaluation hands. Verify the parent failure state and dead process,
artifact hashes, frozen checkpoint hashes, 266,229 control and 266,071 treatment
physical-hand accounting, both PASS five-opponent session audits, exact 4,096
pair indices in each of fifteen raw JSONL files, and identical deck order across
all cells. Recompute the originally preregistered per-deck five-anchor-average
treatment-control and treatment-source estimates and per-anchor contrasts. Apply
the unchanged gate: both aggregate 95% CI lower bounds above zero and at least
3/5 positive treatment-control anchor point estimates. Preserve the parent as
FAILED. Independently repeat all raw calculations before closing this recovery.
New training/evaluation/Slumbot hands are all zero; source evidence comprises
122,880 already-completed evaluation hands.
