# Bounded reporting repair and impact inventory

Zero new poker executions. Preserve all historical reports and frozen source.
Qualify an isolated explicit-panel summarizer: missing/extra anchors, zero-sample
buckets, duplicate identities/decks, nonfinite rewards and bad arithmetic must
fail closed. Gate coverage is an explicit caller contract, not a hidden four-name
constant. Tests cover one/four/five/eight-anchor panels and the reproduced defect.

Recompute both stages/seeds/arms of the completed family-allocation trial using
raw gzip evidence and compare every real anchor/seat/pooled statistic against
the independent terminal correction. Inventory historical stage analysis JSON
and raw-review/controller summaries for zero-sample by-anchor entries. Inventory
findings are suspect reports, not automatically invalid policies or training.
Preserve a hash-bound scope list and disclose exclusions. Do not retrofit imports
into old frozen controllers. Future multi-anchor controllers must opt into this
qualified explicit-panel path with integration tests before starting hands.
