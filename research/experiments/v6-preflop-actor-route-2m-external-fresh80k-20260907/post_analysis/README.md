# One-shot terminal evidence followthrough

This directory is outside the active external executor's frozen source set.
The original controller20736/create1788755792.878595 remains the sole logger
writer while live. Deferred commands and these artifacts belong to the SAME
experiment record after controller and helper are terminal.

The reviewer is the unchanged qualified prior1M four-policy implementation,
bound through the current run_pair and pair_protocol to the four2M endpoints
and new32-session schedule. Its tests use synthetic data only. The helper is
the same one-shot prerequisite chain with the exact current owner identity.
It waits through observation failures, admits only all41 successful jobs and
the fixed80000 hands, runs one review, and never retries or requests poker.
No competing manual review may run while this helper lives or after its report.

Deferred actual commands:

    python -B research/experiments/v6-preflop-actor-route-2m-external-fresh80k-20260907/post_analysis/qualify_review.py
    python -B research/experiments/v6-preflop-actor-route-2m-external-fresh80k-20260907/post_analysis/qualify_followthrough.py
    powershell -NoProfile -ExecutionPolicy Bypass -File research/experiments/v6-preflop-actor-route-2m-external-fresh80k-20260907/post_analysis/launch_followthrough.ps1

Qualification reports preserve actual nested commands, sources, XML and timing.
The helper records its own and review-child identities, commands and exits.
After terminal completion inspect its preserved result, analyze both seeds
and arms against the retained1M development cohorts without new poker, then
finish this same record with accounting, hashes, exact commands and audit.
Do not promote a model or allocate more training automatically.

The session prefix is consistently v6_actor_route_2m_external_20260907 in
the preregistration, executable schedule and frozen launch manifest. The prior
experiment's prose/executable naming deviation is historical, not repeated here.
Raw/session/wave intervals remain conditional and do not establish arbitrary
server RNG independence, training-seed population strength or final qualification.
