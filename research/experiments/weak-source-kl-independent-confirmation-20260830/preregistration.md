# Frozen weak-source-KL independent confirmation

Registered before any new evaluation. Discovery parent weak-source-kl-pilot-20260830
completed531842physical training hands and73728sampled internal hands, passed its
registered gate and independent review. Those hands are excluded here. No training,
optimizer update, reference refresh, policy conversion or Slumbot play is planned.

## Frozen inputs and independent streams

Use exactly the parent's source, final control and final weak checkpoints. The
control SHA is9bffa0a8628725371d06d1c115de21cdd7e0bc2e45dcf92db140e4796a3fcbeb;
weak SHA is a68ac47aad7fa943f0e784e1c14be3d742fac7390a2cd853ba6cb971b429019b.
Original source and all three anchors retain the exact identities in the runner.
No archive endpoint may replace them.

New deal/action seed20260909, distinct from discovery20260908 and training20260907.
Before registration, repository research-memory search found no prior use of the
new seed. Use8192mirrored pairs per anchor per candidate, all3anchors and3candidates:
147456new internal hands. Native sampled_both_sides,temperature1,200bb and the
unchanged physical-seat-keyed SHA256 action stream. Anchor-specific offsets remain
1000003. Both seats and matched deals define each statistical pair.

## Fixed replication gate

Primary family: weak-minus-control across the3anchors. Require all3positive point
estimates and at least1positive Bonferroni98.333% lower bound. Also require all3
weak-minus-original-source point estimates nonnegative and every integrity/OOD
check valid. Report all3primary deltas with nominal95% and adjusted intervals,
plus descriptive source contrasts. Do not substitute nominal significance,
best-anchor selection or pooled discovery data for this gate.

This repeats the pilot gate with independent evaluation randomness and twice the
pair budget; it is not an independent training-seed replication or unseen-opponent
test. These anchors were used in training. Same seeds across candidates supply
paired randomness, not identical trajectories. OOD threshold stays0.15.

If the gate passes, admit only a separately preregistered fixed20k strict sampled
Slumbot pilot of this exact weak checkpoint; do not start100k or training-scale
expansion from this result alone. If it fails, do not extend/reselect/pool the
confirmation. Reassess the general learned-weight mechanism from all contrasts.
The long-term goal remains one frozen policy on at least100000fresh Slumbot hands
with bb/100>0 and95%CI lower bound>0.

## Evidence and stopping

Capture source copies, dirty patch, hashes and exact commands before evaluation.
Save every raw pair/seat outcome and per-candidate execution result. Update the
same record after each complete candidate. Validate counts, identities, streams,
pair/seat arithmetic, descriptive statistics, OOD decision counts and independent
paired-CI recomputation. Recheck all input/source hashes after evaluation.

Fixed final endpoints and fixed budget only. On an infrastructure failure preserve
all files and report it; no automatic restart, partial-cell overwrite, new seed,
supplement or optional extension. Synthetic contract tests count zero poker hands.
The runner writes an analysis and a report but leaves finish to post-exit review,
so the experiment cannot be closed while its original writer is live.
