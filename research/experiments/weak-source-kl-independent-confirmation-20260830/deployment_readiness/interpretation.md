# Scope of the zero-hand deployment check

The exact weak checkpoint loaded through this confirmation's captured Slumbot
client on CPU as model/sample/temp1,200bb,v4 and strict persistent transport.
No network call was attempted and no poker hand was played. The readiness result
must not be substituted for the pending independent-confirmation gate or for a
real external policy-execution audit. All production source/model hashes remain
unchanged. Optional guard arguments in the dry-run metadata are inactive because
the selected policy_mode_raw is sample, not a guarded selector.

If independent confirmation passes, retain its registered next branch: a separate
fixed20k strict sampled Slumbot pilot of this exact checkpoint. Do not mix prior
internal outcomes, old greedy external hands, the aborted native-sampled run or
the later complete source baseline into those20k hands or a qualifying100k run.

For contextual performance reporting, the completed strict native-sampled source
baseline is -48.9778bb/100 over20000hands,95%CI[-71.6649,-26.2907]. The older active
Standard10 reference of-11.4275bb/100 is greedy, a different executed policy.
Label these separately; do not treat a baseline_delta against the greedy result
as a matched sampled-policy treatment effect. This note does not change the
repository's global baseline setting.

An unpaired historical comparison is not the same as a simultaneous randomized
control. The actual benchmark criterion is absolute raw bb/100>0 with a95%CI
lower bound>0 on at least100000fresh hands from one frozen policy; being better
than either historical baseline alone cannot establish success.

Sources: the completed standard10-strict-sampled-slumbot20k-20260830 result_summary.md;
this experiment's preregistration.md and deployment_readiness/zero_hand_result.json;
the prior standard10 baseline's distinct greedy-policy history.
