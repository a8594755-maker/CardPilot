# Fixed fresh-only versus replay current-regimen trial

Single primary comparison: replay ratio0.5(control) versus0(expanded directory
label means fresh-only, not pool expansion). Both capacity9, rolling buffer2,
same original parents/Adam/LR/static reference/connected actor/full network,
sampled hero and opponents, frozen greedy physical200bb legacy_v4 evaluation.
No temperature, opponent allocation or policy objective change.

Seeds1/3 original flat family-allocation stage2 roots15744527/15747327 physical
hands; never smoke descendants. Stage1 adds262144; stage2 adds1048576 cumulative
from root, whole-iteration overshoot counted. Order S1control,S1fresh,S3fresh,
S3control, reversed at stage2. Max7200seconds/job,no retries or counter reset.
Statistical managed resume, not bitwise worker RNG equivalence. Fresh-only keeps
historical replay cumulative count and RNG while rolling current buffer forward.
Equal physical hands need not mean equal optimizer steps/wall cost: report both.

Eight fixed evaluator checkpoints: original four preservation anchors plus two
prior mixture0.5 endpoints and two heads-only endpoints, chosen by recipe/seed
before new outcomes. Qualification must establish distinct tensor identities
from current two root models/pools and recorded external sources. Sibling shared
ancestry remains; this is not proof of lifetime non-exposure or universal breadth.
No opponent selection by observed strength. Evaluate both seeds/arms/root+endpoint,
1024 paired decks per anchor,2seats,32768 executions/job,131072/stage,262144total.
Seed20267400+10*seed+stage; anchor offset1000003. Stage seeds distinct and raw
decks excluded from retained common-deck corpus. Use explicit-panel summaries.

Continue262k->1M unless preservation or transfer panel has >=3 anchors with
upper95CI below-25bb/100 and both seat upper CIs below0, for contrast or either
endpoint-minus-root. This severe-collapse check is not a claim of improvement.
Report all contrasts/own-root/anchors/seats, paired conditional normal95CIs,
actual Adam steps/replay/physical/transition/wall cost. No aggregate-only promotion.
No new Slumbot or automatic4M/final qualification. After fixed dose, independent
raw/state/hash/deck/assignment review then decide scaling from replicated curves.
