# Opt-in preflop actor route qualified; no poker-strength claim

Both retained original-LR final parents passed exact metadata-only derivation,
strict model/Adam load and serialization checks. All original model tensors,
86 optimizer states/clocks, replay entries/RNG/exposure, pool history, physical
and transition counters, main RNG and managed-attempt evidence remain intact.
Only the preflop gradient configuration and explicit migration provenance change
in the saved unstepped treatment parents. Production sources remain unchanged.

Qualification report SHA256:
`a5f6cfce6bc32535cc4f43231b69f30cd1d7e2ab748da175af4f65f7d88a705c`.

| Seed | Physical hands retained | Iteration | Unstepped treatment SHA256 |
|---|---:|---:|---|
|1|10498234|2215|799057d2d117302304e0c57bc6162840f2920c31623f8712b0c2125e26cceff5|
|3|10492141|2218|8c78563b97681c4af9b526fdeebcde704acee4e50cc0768cb9804e007e965c9c|

Forty-three focused tests and375 copied-runtime regressions passed. Module bindings
confirmed candidate code was actually exercised. Real CLI checks admitted the new
help option and rejected missing separate head / active teacher loss. Resume flag
disagreement and corrupt flag metadata fail closed. The default path is unchanged.

On64 retained internal replay decision states per seed,16 per street, original,
candidate-default and candidate-connected forwards matched bitwise before updates.
States were selected by first occurrence per street, not winnings or Slumbot labels.
Actual actor cross-entropy gradients reached preflop head only in default mode,
and preflop head plus shared body in connected mode. Postflop routes were unchanged;
critic_v2 reached only its value head on every street in both modes.

Two paired diagnostic update fixtures loaded actual1e-4 Adam state, advanced every
one of86 parameter clocks once and differed only in shared-body parameters. Head
parameter updates were bitwise equal. Body difference norms0.363423/0.373807 are
fixture measurements, not poker gains or production PPO doses. These diagnostic
models were not saved or promoted. Saved treatment parents remain unstepped.

The preflop route applies to PPO, entropy and existing source-KL actor graphs; it
does not isolate the reward gradient alone. Existing critic detachment is retained.
Missing preflop connectivity is established, but it has NOT been proven to cause
the prior negative external results or to be inferior to protective detachment.

## Preserved qualification failures and source versions

The first isolated runtime omitted the unchanged deep_cfr dependency and produced
73 collection errors. The23 dependency files were then copied with hashes into
both original/candidate snapshots; the distinct V2 run passed all375 tests. The
original regression runner, output and XML remain. No poker hand was executed.

Qualification V1 then stopped before any parent/model load because Windows cp950
could not decode UTF-8 source while generating a diff. Original source and failure
report remain. V2 changes source reads to explicit UTF-8 and uses separate outcome
paths; it passed in43.203 seconds. No passing poker work or unit suites were rerun.

The initial protocol source (SHA602235fa41b84f9ba7e2afec396223c35b6c09ce49ad63dacbe9e703d938fbf3)
is retained exactly as protocol_initial.md. Before qualification the protocol was
clarified to state that entropy/source-KL actor gradients share the opt-in route;
no budget, parent, critic, inference or experimental outcome was changed.
Both source snapshots, candidate_delta.patch and the new contract module are bound
by the qualification report. Original snapshot contains286 Python files; candidate
contains287: two edited sources and one new small contract module.

## Decision

Complete this qualification record and preregister a two-seed connected-versus-
detached geometric trial using BOTH current original-LR parents. No LR scan,
selected lucky seed, critic-route change, new architecture head or Slumbot test.
Keep all other production settings matched. Reuse verified execution/resume/raw-
evaluation helpers, and require the actual first resumed GPU checkpoint to preserve
optimizer/replay/pool/main-RNG/counters and the route/migration metadata. Synthetic
qualification is not actual worker resume equivalence. Every new arm/stage needs
its own managed attempt namespace; statistical continuation is not bitwise worker
RNG continuation. No automatic paper-scale budget or final qualification.

New environment training hands0; evaluation hands0; Slumbot hands0; final
qualification hands0. Retained replay rows and diagnostic gradients are not new
environment hands. Goal achieved:false. No stronger policy has yet been produced
by this backward-route implementation alone.
