# Opt-in preflop actor representation gradient qualification

Registered after the completed four-policy external calibration. This is a
bounded implementation/qualification step, not another Slumbot evaluation or a
claim that missing connectivity caused losses. Keep both original-LR final
parents from v6-full-step-size-1m-continuation-20260905: Seed1 SHA8904f3b2e25baec5bc0bcb6556502c19b3fb213efdc9a32b16d55eee1284a3ef
and Seed3 SHA f2249b0d19937ddadfc29fe5ba10cd7f07909d638dc0edeca89fae05b6f498e2.
Do not choose only the better external seed or restore an earlier lucky stage.

Hypothesis: allowing the learned preflop actor objective to train the shared
representation could improve adaptation under the current full-scope RL regimen.
Existing separate-head detachment was designed for teacher isolation. It remains
the default and major control, not a bug retrospectively invalidating old runs.
Critic_v2 remains detached. Forward logits/value/actions must be exactly unchanged
for equal weights before training; only backward connectivity may change.

Stage all Python runtime sources in this experiment. Preserve original source
snapshots/hashes and modify only candidate network/trainer plus a small gradient
contract module. No shared production cutover or live process source mutation.
Add an opt-in preflop_trunk_gradient flag with fail-closed resume agreement.
Limit opt-in admission to learned model preflop with no teacher loss and full
parameter scope. Preserve all old default paths and tensor/state-dict shapes.

Qualification: focused default/opt-in parity and street-specific actor/critic
gradient tests; negative CLI/resume-contract tests; applicable full copied-runtime
regressions; actual retained checkpoint strict model/Adam restoration and unstepped
serialization of metadata-only derived treatment parents. Compare every checkpoint
field except explicitly changed flag/provenance, including all86 Adam states and
their clocks, replay, pool, counters and main RNG. Synthetic gradient/update
fixtures are diagnostic only and never promoted or counted as environment hands.

New physical training/evaluation/Slumbot hands in this qualification: zero.
The original files and every unsuccessful attempt remain evidence. No retries that
overwrite outputs. Finish this same record after qualified result or terminal
failure; a later production pilot must separately preregister exact commands,
seeds, managed attempt namespaces, geometric dose and untouched evaluations.
No actual worker resume equivalence or stronger poker claim follows from unit
tests. That initial production-state audit remains necessary in the later pilot.

Do not repeat earlier gradient-route observation or broaden to critic gradient,
new action rules, Slumbot labels, imitation teacher, LR tuning or new heads. This
one change replaces the unreplicated half-rate comparison as the next research
control; it does not establish an extra concurrent training family.
