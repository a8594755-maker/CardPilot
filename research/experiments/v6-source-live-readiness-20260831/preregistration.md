# Corrected-v6 source live protocol readiness (not a strength test)

The ten v6-prefixed research records currently contain no real Slumbot hands.
Old external baselines use a different execution contract. Offline journal/client
readiness does not prove compatibility with the real service. This fixed probe
tests that remaining boundary using the original source binding, not either new
league candidate or any previously failed candidate.

## Immutable scope

Exactly two sequential fresh sessions, eight hands each, source checkpoint SHA256
944f5f6625e29c2d2562cc457206aafcf840ef0c7edd6accbd372e1362dd57b2.
Copy the existing v6 source artifact without modifying tensors or metadata.
Session IDs v6_source_live_20260831_s01/s02; policy seeds2026092801/2026092802.
Use the already offline-tested journaled client from an immutable source snapshot,
CPU1thread, strict native sampled policy/temperature1, explicit v6 physical-chip
contract. Only slumbot.com new_hand/act POSTs through the existing verified-TLS,
no-retry persistent transport. No login, real-money wager, custom actions, policy
changes, fallback, overwritten artifacts, retries, resumed sessions or replacements.

Before live requests: verify the17-test offline readiness record and its76source
copies; verify the ongoing league matrix's77source copies and frozen identities;
capture this probe's SHA/patch/source copies; run fixed orchestration tests and a
zero-hand snapshot-only loader check. There must be no active training or other
external poker client. The existing six CPU internal evaluators may continue;
their frozen policies, configuration, sessions and hand evidence are untouched.
This adds one CPU client, sequentially, not additional matrix jobs.

After session1, perform the full independent journal audit with exact frozen-model
decision replay. Start session2 only if session1 exits0, all8raw hands validate,
server counters/totals match, and snapshot/checkpoint identities remain unchanged.
After session2, also audit disjoint complete token chains, distinct session IDs,
seeds and journals. Distinct tokens do not prove server RNG independence.

Any observed client/protocol/evidence failure stops this probe without retry or
additional session. Preserve every request intent, response, raw hand, summary and
exit. Record valid durable raw hands as evaluation_hands/slumbot_hands, and report
attempted hands, observed server terminal claims and ambiguous requests separately;
never label an unknown or unvalidated server result a valid benchmark hand.

Success means only observed live protocol/evidence readiness on these16hands.
Report observed coverage, not universal API compatibility. Do not compute a
strength CI, select a policy, pool these hands into a later baseline, or admit100k
qualification from this tiny probe. A separate fixed fresh baseline calibration
must be preregistered next if readiness passes. No strength claims or relaxation
of any existing candidate gate: the ongoing245760hand league matrix completes as
specified, with zero Slumbot hands in its own record. Earlier offline notes about
candidate confirmation remain applicable to candidate promotion; this is only a
separate source-protocol calibration.

Offline readiness analysis SHA256:
8d905b413e6417543e727df7edd24846839127caad2ed296a9bbe24791eb29b6.
No new environment training hands or offline synthetic poker trajectories here.
