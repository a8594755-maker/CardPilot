# Current actor probe decision

Completed36221 new physical executions,32967 transition hands,60743 replay rows;
four iterations per seed, eight observations per seed (first minibatch in each
of two PPO epochs). No strength evaluation or final Slumbot hands. Independent
saved-state/suffix/hash review passed. Statistical managed continuation only;
do not call worker rollout bitwise identical or all training deals proven unique.

Weighted reference-KL/PPO gradient norm ratio median1.838(seed1),2.087(seed3).
Whole actor/PPO cosine median0.674/0.587 and minimum0.248/0.398; zero negative
whole-actor cosine observations. This does not support global PPO cancellation
in these minibatches. Trunk cosines also all positive. Seed3 policy head has one
negative actor/PPO cosine(-0.160); this is local, not replicated global failure.
Norms/cosines precede clipping and Adam and do not measure effective step size,
gradient signal/noise or causal poker improvement. Larger KL norm alone is not
a justification to lower its coefficient.

Two controller deviations preserved: first attempt failed before trainer launch
because the inherited command had no diagnostic option; its files remain under
preflight_attempt1 names. Seed1 then cleanly finished17617 physical hands before
an inherited long-dose requirement for two newly sampled snapshot identities
failed. That requirement is inapplicable to a four-iteration observer. Preserved
its actual exposure, independently checked saved state, and continued only Seed3
(18604 hands), never reran Seed1. No diversification claim is made from the probe.

Decision: no KL/objective change justified by this local evidence. Choose one
bounded unchanged-regimen extension of the original fresh-trial replay-control
final parents, seeds1/3, adding1048576 physical hands each. Do not use the probe
descendants as silent replacements. This moves that fixed comparison from about
1M to about2M incremental hands; historical cumulative counters span earlier
recipe changes and are not a stationary algorithm's entire learning curve.

Rationale is unresolved scale under active, non-collapsed updates, not proven
positive slope. The retained scores and latest matched cohort remain uncertain;
do not describe this allocation as validated improvement. Keep a single trained
lineage family and frozen controls, no new hyperparameter arm. Before launch
freeze multi-anchor/both-seat comparisons to both the extension start and earlier
fixed-regimen root, fresh deck seeds, compute cap and resume contracts. At the
fixed2M endpoint require replicated directional breadth and cost evidence before
considering4M; no automatic4M, external test or paper-scale commitment. If the
longer unchanged curve still fails to supply useful learning direction, change
the learning approach rather than open another pool/replay/ensemble sweep.
