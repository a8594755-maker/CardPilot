# Second same-record checkpoint-publication interruption

At 2026-09-05 10:41:34 UTC, moving256_stage1 exited 1 after the five-second
same-payload Windows replacement retry failed. The recovery controller and all
observed descendants are terminal. This is an infrastructure interruption, not a
poker-strength result or permission to change the algorithm, seeds or targets.

The original published latest.pt and the fully serialized, fsynced candidate
`.latest.pt.c836605996e94811862c30e5da30ad98.pending` are both preserved. No overwrite,
rename, deletion, restart or counter reset is authorized by this amendment alone.
First validate the candidate against raw metrics, assignment history, optimizer,
replay, moving-reference state, main RNG and its attempt receipt. If valid, derive
a separately documented byte-identical parent rather than re-executing its update.
Unknown additional worker tails remain unknown; do not infer zero.

The earlier interrupted static prefix and completed static remainder remain in
the same experiment. The 4,292 observed but unretained executions of that FIRST
interruption remain separately counted. All previous source, qualification,
status and error evidence must stay unchanged.

File attributes do not show read-only protection. Access-denial provenance is
unresolved. Any lock inspection is observational only: do not kill applications,
change ACLs, disable security software or bypass permissions. A present-day empty
lock list cannot identify or exclude the failure-time holder. Qualify any further
I/O change with actual Windows sharing conflicts before a new trainer launch.

The previously qualified one-interruption post-analysis must not be run unchanged
as this twice-interrupted experiment's final report. Preserve it and its tests as
historical qualification; final accounting needs both interruptions and every
completed/recovered attempt. There is still only one research experiment record.
