# Independent Seed3 replication of the same reference-regimen dose

Prospective training/evaluation protocol after the complete, fixed Seed1 two-arm
external development comparison. No hands in this record have yet been executed.
This is one family and one main comparator, not an architecture/opponent sweep.

## Evidence and question

Seed1 final internal moving-minus-static was+14.9470bb/100,95% CI[-5.2945,35.1886];
external40k/arm was-9.6023,CI[-32.2884,13.0838]. Neither method won Slumbot and no
method superiority or broad external collapse was established. Three balanced
external waves favored moving in point estimate; the third strongly favored
static. Retain all four. These observations justify uncertainty-reducing
independent-seed replication, not a claimed aligned proxy or automatic16M scale.

Continue the predesignated incident-unaffected original Seed3 4M parent under the
SAME static-versus-phase-held package, to learn whether Seed1's geometric signal
repeats. Seed3's previous2M-to-4M internal slope was-1.9981 with wide CI, not a
best-performing seed selected for promotion. Seed2 is excluded because its known
repeated-deal incident compromises clean freshness interpretation. This does not
certify every older boundary of either retained seed or unknown pretraining data.

## Parent, seed bindings and resume contract

Parent `v6-static-current-kl-4m-scale-20260904/seed3/latest.pt`, SHA256
`ba4ed0a5761f3b804053713f5246c20faced8a6949c969e9e6557033e1ae2810`, iteration880,
physical4,194,908, transition-bearing3,624,053, replay rows6,383,843. Both arms
start from this exact checkpoint, not a Seed1 derivative or newly reset model.

Training seed20263003, worker seed base2026300300, fixed-deal start39300000 and
lineage run ID `v6_nashpg_static_seed3_20260903` remain Seed3's original values.
Reserve a fresh durable namespace for EVERY attempt before workers, preserving
all earlier namespace evidence and preventing repeated deal-prefix reuse.
Within the experiment training arms share initial state but use different deal
namespaces; they are not common-deck paired training or bitwise continuations.

Preserve optimizer and effective LR1e-4, all saved replay entries/RNG/cumulative
rows, league history, hand counters and full opponent assignment evidence. The
legacy parent lacks `main_process_rng_state`: explicitly record the first managed
attempt's legacy main-RNG bootstrap, not a false exact RNG restoration claim.
Subsequent managed checkpoints DO serialize main RNG; require exact restored
state then. Reconstruct assignment RNG from the complete preserved trace, never
trim a pending assignment or silently overwrite old logs. Actual initial saved
model/optimizer/replay state must compare exactly before training can be credited.

The old4M generic training audit failed its capped-pool-history test; preserve it.
Use the separately qualified archive-window and inherited-bridge/mechanics proof
and its explicit incident-unaffected Seed3 qualification, not a fabricated passing
historical audit. Older-boundary freshness is not certified by that proof.

## Unchanged algorithm package and budget

Use frozen production trainer SHA256
`1b4d23abfa3e0a9c675126fd7183d37b134a2a13253563e990e144cc01a90f8b`.
12workers,multi8,4096transition-bearing hands/update, all policy heads plus critic_v2,
frozen trunk, sampled heroT1, KL(current||reference) coefficient1, PPO2,targetKL.01,
minibatch16384,replay depth2/ratio.5,loss-K-best5,25% current self-play,eight adaptive
groups and the same three training anchors. CLI lr.0003 does NOT replace the
preserved effective optimizer LR1e-4.

Static keeps Standard10 SHA91b0c587a5a76e9a8f38217e0b304136ef298118a99b69cc743899fc4b16e428.
Moving rebases its reference to the exact Seed3 4M actor and refreshes every256
completed updates. Reference activation is iteration880, with boundaries1136,
1392,1648,...; do not reuse Seed1's activation882. This tests rebasing plus cadence,
not cadence alone or a faithful full-network AlphaHoldem/NashPG reproduction.

Stage1 physical target6,292,060 (parent+2,097,152); stage2 target8,389,212
(parent+4,194,304), EACH arm. Planned new physical executions8,388,608 across the
two arms plus measured overshoot. Order static1,moving1,stage1 evaluation,moving2,
static2,stage2 evaluation, as in Seed1. One GPU training/evaluation owner at a time.
Separate new physical hands, transition hands, replay rows, no-decision hands,
worker tails, shared parents and any unknown crash suffixes. Two arms' total is
not one policy's dose or independent pretraining. No paid compute.

Each attempt retains the7200second guard. A natural below-target stop is a safe
boundary requiring researcher review, not an automatic restart or method failure.
Never rerun completed hands or overwrite an interrupted attempt. Preserve unknown
crash suffixes as unknown. The previous measured training cost suggests roughly
five training hours plus evaluation for this total dose, not a guaranteed ETA.

## Frozen evaluation and decisions

Use the same four frozen anchors and generic greedy execution as Seed1. At each
stage evaluate both endpoints versus the same original Seed3 parent on2048 fresh
decks per anchor, both seats. Evaluation seeds20263511(stage1),20263512(stage2).
Join raw anchor/deck/seat identities; require exact repeated-parent outcomes and
prior-corpus deck disjointness. Execute65,536physical internal hands/stage,
131,072total, including intentional duplicate parent controls. Report conditional
paired intervals, per-anchor/per-seat breadth and raw moving-minus-static, not
independent-sample intervals for the intentionally shared parent executions.

Standard10 drift uses20,000states/arm/stage, seeds20263521/20263522,80,000total.
Drift is not strength. Keep weights and inference contract fixed during evaluation.

Reuse the original practical collapse gate before stage2: moving-minus-static
upper95% bound below-25 in at least3/4anchors AND negative upper bounds in BOTH
seats. Otherwise complete the planned geometric dose despite uncertain or
negative early points. Stop/preserve for mechanical, nonfinite, source, counter,
reference, namespace or evidence failures. No intermediate external screening.

After completion, combine Seed1 and Seed3 conditional evaluation results, show
each seed and an equal-seed summary, and explicitly retain the very small n=2
training-seed uncertainty. Do not call thousands of evaluation deals independent
training-seed replicates. Examine the later-stage slopes and external translation
limits before deciding further dose. No automatic16M expansion, Slumbot run or
model selection. Any subsequent external calibration must be separately fixed
and preregistered, not chosen from a lucky5k or a best-seed endpoint. Final>=100k
qualification remains separate; no hands here qualify it.

## Implementation qualification

Reuse experiment-scoped copies of the already qualified controller/evidence/tests,
changing only parent/seed/deal bindings, activation iteration and evaluation seeds,
plus a fail-closed parent preflight. Preserve every original source file. Run the
offline controller/command/Seed3 binding tests and parent checkpoint proof before
launch. Exact first command:
`python -B research/experiments/v6-phase-held-reference-seed3-replication-20260905/qualify.py`.
Launch separately through hidden file-backed `launch.ps1`. The live controller
owns this experiment's logger and frozen dependencies until its exact PID and
creation time are terminal.
