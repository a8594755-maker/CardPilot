# Current external run followthrough

This directory does not change frozen policy/runtime/session evidence. The
controller alone owns the experiment logger while PID51436/create1788709116.2517476
is live. Do not restart its32 fixed sessions or rename the executable session IDs.
The prose-prefix discrepancy is preserved in naming_deviation_outcome_blind.md;
all actual IDs/seeds were fixed and checked before requests.

review_completed.py passed33 new offline tests on2026-09-06T15:37:47UTC. Do not
rerun this qualification just to monitor progress. It refuses live owners, live
children, incomplete coverage, failed jobs or an existing output. It independently
reaggregates raw integer payout moments and checks full replay/session evidence.

terminal_followthrough.py is a one-shot, exact-owner wait followed by this
qualified review, with no new poker requests, model updates or logger writes.
Its10 offline tests passed2026-09-06T15:43:26UTC. The complete qualification binds
the naming-deviation note and the unchanged pre-request launch/session documents.
It retries observation errors against the SAME owner, never poker attempts. On
failed/incomplete execution it preserves everything and refuses the review.

Executed setup commands (cwd C:/Users/a8594/CardPilot):

```
python -B research/experiments/v6-preflop-actor-route-two-seed-external-fresh80k-20260906/post_analysis/qualify_followthrough.py
powershell -NoProfile -ExecutionPolicy Bypass -File research/experiments/v6-preflop-actor-route-two-seed-external-fresh80k-20260906/post_analysis/launch_followthrough.ps1
```

The nested pytest argv is retained in followthrough_qualification.json. The actual
helper and review argv/identities are retained in followthrough_execution.json.
After launch, inspect exact helper identity and state. Do not manually launch a
competing review while the helper is waiting/running or if its report exists.

When controller and helper are both terminal, verify the raw review report and
all input bindings, then make the substantive cross-seed/seat/cohort/internal
comparison. Attach these deferred commands/artifacts and the naming-deviation
note to THIS experiment, update exact accounting/costs, finish via experiment_log.py
and audit. No final100k credit, next training, model selection or goal completion
is authorized by this helper. Any later failure needs evidence-based analysis,
not a blind restart. Original preparation80 tests, endpoint parity failure and
corrected256-row bridge parity remain preserved in the experiment root.

## Additional terminal-only parent preservation context

compare_parent_transfer.py and its10 tests were qualified at2026-09-06T15:50:33UTC,
without reading current outcomes or generating hands/model queries. The original
two parent weights and unchanged external runtime were independently matched
against the completed historical review. This is an exploratory historical-cohort
comparison, not a simultaneous randomized contrast. The two endpoint comparisons
within each seed share the same historical parent and are correlated.

The10-test qualification retains one SciPy RuntimeWarning from the deliberate
constant-value synthetic treatment fixture; its independent Welch interval still
matches the moment formula. No real current poker result was involved. Do not
erase this warning or rerun the completed qualification merely to remove it.

Already executed (defer logging until both owners are terminal):

```
python -B research/experiments/v6-preflop-actor-route-two-seed-external-fresh80k-20260906/post_analysis/qualify_parent_transfer.py
```

After BOTH controller and exact followthrough PID58116/create1788709462.2370713
are terminal, and the qualified raw review exists, run this once:

```
python -B research/experiments/v6-preflop-actor-route-two-seed-external-fresh80k-20260906/post_analysis/compare_parent_transfer.py
```

It writes parent_transfer_comparison.json exclusively, with no logger writes or
automatic direction/scale decision. It has NOT been run during qualification.
Retain the actual command and all qualification/source/XML artifacts on this
same record at closure. Frozen evaluator, model, session and test-source bindings
have not been edited for this supplemental analysis.
