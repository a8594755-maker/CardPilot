node_modules
dist
build
coverage
.env
.env.*
!.env.example
.env.local
.env.*.local
.DS_Store
*.log
*.bak_corrupt

# Local Netlify folder
.netlify

# Local dev data (JSON fallback persistence)
.data

# Supabase egress budget tracker (auto-generated)
.egress-tracker.json

# CFR solver output (large strategy files)
data/cfr/

# CFR model files (large/experimental ones stored in iDrive e2)
models/*
# Production models (used by bot decision pipeline + resolver)
!models/vnet-v91-balanced.json
!models/vnet-v91-balanced-metrics.json
!models/vnet-v2-pipeline.json
!models/vnet-v2-pipeline-metrics.json
!models/vnet-v3-srp-100bb.json
!models/vnet-v3-srp-100bb-metrics.json
!models/vnet-v4-3bet-100bb.json
!models/vnet-v4-3bet-100bb-metrics.json
!models/vnet-v5-3bet-50bb.json
!models/vnet-v5-3bet-50bb-metrics.json

# Training data (large JSONL files)
data/training/
data/v2/

# Self-play config state
data/self-play-panel-config.json

# Logs
logs/

# Temp files
NUL
_test-compile.ts

# Cluster config (contains API tokens)
packages/cfr-solver/scripts/cluster.env

# Large archives
*.tar.gz

# EZ-GTO solver data (separate from submodule)
EZ-GTO-data/

# ML checkpoints and tools
checkpoints/
tools/

# Python cache
__pycache__/

# Preflop solution outputs (large, generated)
data/preflop/solutions/
data/preflop/training_*.jsonl
data/preflop/validation_*.json

# NN training data
data/nn-training/
data/compare_gtoplus/

# pnpm lockfile (project uses npm)
pnpm-lock.yaml
apps/web/pnpm-lock.yaml

# Python requirements (tracked separately)
requirements-nn.txt

# Pipeline operations scripts (local, machine-specific)
scripts/*.ps1
scripts/*.bat
scripts/*.sh

# Python monitoring scripts (local ops)
scripts/*.py

# EZ-GTO source (third-party reference)
EZ-GTO/

# Playwright test results
test-results/

# Local agent state and disposable verification worktrees
.claude/settings.local.json
.claude/launch.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.test_tmp/
agent-tools/

# Local research archives and third-party/scratch workbenches
data/archive/
workbench/

# Runtime coordination files
*.lock

# High-volume generated research evidence. Keep compact summaries, records, and
# reproducible scripts in Git; preserve raw per-hand/per-state payloads locally.
reports/_tmp_*/
reports/**/*.jsonl
reports/**/*.jsonl.gz
reports/**/*.jsonl.partial
reports/**/*.signal
reports/**/*.err
reports/**/manifest.json
reports/h11_mirror_001_prepare_failed_wrong_out_20260715.json
reports/v5_lrft_f8r1_preregistration_audit_b35078ee7ad2ab123d5f9b0770538793_20260723.json
reports/v5_lrft_f8r4_preregistration_audit_165822f101c1da7589cb3a605d16abbc_20260723.json

# Large local artifacts — keep out of git snapshots (AI-harness `git add -A`/`git status`
# was hashing ~23GB repeatedly, stealing CPU from the trainer main loop; 2026-07-07)
data/phase2/
tmp/
*.zip
# Model checkpoints & caches accidentally under scripts/ (never commit weights)
scripts/alpha_holdem/models/
__pycache__/
# Bulky historical eval dumps (summaries at eval_logs/ root are kept)
eval_logs/path_b/
