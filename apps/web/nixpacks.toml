[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build -w @cardpilot/web"]

[start]
cmd = "npm exec --workspace=@cardpilot/web -- vite preview --host 0.0.0.0 --port $PORT"
